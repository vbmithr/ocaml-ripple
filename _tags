<**/*.{ml,mli}> : bin_annot
<lib> : include
<src> : include

<**/*.{ml,native,byte}> : package(lwt), \
			   package(lwt.unix), \
			   package(websocket), \
			   package(ppx_deriving), \
			   package(yojson), \
			   package(ppx_deriving_yojson), \
			   package(uri)
